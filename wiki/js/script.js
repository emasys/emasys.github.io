document.addEventListener('DOMContentLoaded', main);

function main() {
    titlebarJs.init({isTop: true, id: 'header', triggerId: 'xx'});
    // titlebarJs.init({id: 'titlebarbottom', isTop: false});
}